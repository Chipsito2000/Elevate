<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página de Error</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* Estilos generales para el body */
        body {
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: 'Arial', sans-serif;
            color: dodgerblue;
            text-align: center;
        }

        /* Estilos para el encabezado del contenido */
        .content-header h1 {
            font-size: 2em;
            margin-bottom: 20px;
        }

        /* Estilos para el contenido */
        .content {
            background: rgba(0, 0, 0, 0.7);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 100%;
        }

        /* Estilos para el titular del error */
        .headlin {
            font-size: 3em;
            margin-bottom: 20px;
        }

        /* Estilos para el contenido del error */
        .error-content h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
        }

        /* Estilos para el ícono de advertencia */
        .fa-warning {
            margin-right: 10px;
        }

       
    </style>
</head>
<body>
    <section class="content-header">
        <h1>No cuentas con los permisos suficientes</h1>
    </section>

    <section class="content">
        <div class="headlin text-red">ERROR</div>
        <div class="error-content">
            <h3><i class="fa fa-warning text-red"></i>Sin autorización</h3>
        </div>
    </section>
</body>
</html>
